const Dolar = { currency: "dolar", abbr: "USD" }
const Real = { currency: "real", abbr: "BRL" }
const Euro = { currency: "euro", abbr: "EUR" }

const Currency = {
  Dolar,
  Euro,
  Real,
}

export default Currency
